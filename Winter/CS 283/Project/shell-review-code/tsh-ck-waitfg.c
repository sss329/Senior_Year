/*
* waitfg - Block until process pid is no longer the foreground process
*/
void waitfg(pid_t pid)
 {

  /* Wait for process pid to stop being the foreground process.
     Find the process in the job list, then
     check its state, goto sleep, repeat
  */

  return;
 }
